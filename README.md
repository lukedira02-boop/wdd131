# wdd131

> Note: For best results, open this site through a local web server (for example `npx serve .` or VS Code Live Server) instead of using `file://`. This avoids browser origin restrictions and prevents image loading/Console errors.